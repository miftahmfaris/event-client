import styled from "styled-components";

const Nav = styled.nav`
    display: flex;
    justify-content: space-between;
    background: black;
    padding: 0 20px;
    color: white;
`;

export default Nav;
